---
- name: enable | Ensure swap file exists.
  ansible.builtin.command: >
    {{ swap_file_create_command }}
    creates='{{ swap_file_path }}'
  register: swap_file_create

- name: enable | Set permissions on swap file.
  ansible.builtin.file:
    path: "{{ swap_file_path }}"
    owner: root
    group: root
    mode: 0600

- name: enable | Make swap file if necessary.
  ansible.builtin.command: mkswap {{ swap_file_path }}
  when: swap_file_create is changed
  register: mkswap_result

- name: enable | Run swapon on the swap file.
  ansible.builtin.command: swapon {{ swap_file_path }}
  when:
    - mkswap_result is changed
    - not swap_test_mode

- name: enable | Set swappiness.
  ansible.posix.sysctl:
    name: vm.swappiness
    value: "{{ swap_swappiness }}"
    state: present

- name: enable | Set vfs_cache_pressure
  ansible.posix.sysctl:
    name: vm.vfs_cache_pressure
    value: "{{ swap_vfs_cache_pressure }}"
    state: present
